/**
 * Карта переключения состояний сортировки
 *
 * Подробнее: Этот объект определяет циклическую последовательность
 * состояний сортировки. При последовательных нажатиях на заголовок таблицы
 * сортировка будет циклически меняться: отсутствие сортировки -> по возрастанию ->
 * по убыванию -> отсутствие сортировки и т.д.
 */
export const sortMap = {
    'none': 'asc',    // Из "без сортировки" переходим в "по возрастанию"
    'asc': 'desc',    // Из "по возрастанию" переходим в "по убыванию"
    'desc': 'none'    // Из "по убыванию" переходим в "без сортировки"
}

/**
 * Карта соответствия полей для сервера
 * 
 * Подробнее: Некоторые поля на клиенте и сервере называются по-разному.
 * Например, на клиенте мы используем 'date', а на сервере это поле может
 * называться 'receipt_date'. Эта карта помогает преобразовывать имена.
 */
export const fieldMap = {
    'date': 'date',      // Поле даты
    'total': 'total_amount' // Поле суммы
};

// ВНИМАНИЕ: Функции sortUp, sortDown и sortCollection больше не нужны!
// Так как сортировка теперь происходит на сервере, мы удаляем их.
// Оставляем только sortMap и fieldMap.